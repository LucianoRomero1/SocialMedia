{% extends "base.html.twig" %}

{% block body %}
    <div class="container mt-2 box-form">
        <h2>Register</h2>
        <hr>
        {{form_start(form, {'action': '', 'method': 'POST'})}}
        {{form_errors(form)}}
        {{form_end(form)}}
    </div>
{% endblock %}